I have a TypeScript class `OrderService` with a `private calculateDiscount(order)` method, and I want to write a unit test that asserts the discount calculation directly. The test cannot reach a private method. Should I just change `private` to `public` so the test can call it? Show me the diff.
